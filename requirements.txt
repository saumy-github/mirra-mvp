# Python >= 3.12 required
# pip    >= 21.0 required

black>=22.0.0
flake8>=4.0.0
mypy>=0.950
numpy==1.26.4
opencv-python==4.10.0.84
Pillow==10.4.0
pandas>=1.3.0
pip>=21.0
pymongo>=4.13.0
pytest>=7.0.0
pytest-cov>=3.0.0
python-dotenv>=1.0.0

# Website backend (FastAPI, website/backend/)
# [standard] includes the `fastapi dev` CLI — the standard dev-run command
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.5.0
PyJWT>=2.9.0
bcrypt>=4.2.0
python-multipart>=0.0.9
email-validator>=2.2.0
httpx>=0.27.0

# 2D pattern generation (DXF export for CLO3D)
ezdxf>=1.0.0

# CLO3D REST plugin client
requests>=2.28.0

# Phase 2: GLB post-processing (texture injection into exported simulation GLB)
pygltflib>=1.15


scikit-learn==1.5.1
scipy>=1.7.0
tensorflow>=2.3.0
torch==2.4.1
torchvision==0.19.1
# 4.56.0 is the first release with SAM2 (Sam2Model/Sam2Processor) support,
# used by product_ingestion/segmentation.py's primary segmentation method.
# Requires torch>=2.2 (satisfied by torch==2.4.1 above).
transformers==4.56.0
# U2-Net fallback for product_ingestion/segmentation.py when SAM2 is
# unavailable or produces an out-of-range mask (segment_u2net()).
# Pinned to 2.0.67, not the latest (2.0.77): 2.0.70+ requires numpy>=2.3.0,
# which conflicts with numpy==1.26.4 above (needed by opencv/scikit-learn/
# torch). 2.0.67 is the most recent release with no numpy constraint at all.
rembg==2.0.67
trimesh>=3.9.0
