# Coding Style Guide

Define formatting rules, naming conventions, and code patterns to maintain consistency.

## Formatting

## Critical Rules

> [!IMPORTANT]
> Add any non-negotiable style rules here. These override defaults.

Example from your user rules:

1. Keep imports compact - Don't expand them to multiple lines
2. Do NOT change single quotes to double quotes or vice versa
3. Do NOT add extra spaces or remove existing spaces
4. Do NOT add extra line breaks or remove existing line breaks
5. Follow the EXACT formatting style of the existing file
6. Keep ternary operators on one line
7. Do NOT create walkthrough documents unless explicitly requested by the user
8. Always run project commands from the repo root `mirra-mvp/`
9. For Python project commands, use repo-root relative script paths such as `python clo_avatar_generation/run_vto.py`
10. Do NOT use the `python -m` format for project commands
11. Follow this command format for both existing commands and any new commands added later
